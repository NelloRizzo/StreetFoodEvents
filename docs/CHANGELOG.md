# CHANGELOG — Street Food Events

Changelog del mese corrente (in ordine cronologico).
Le voci piu vecchie sono archiviate in CHANGELOG_ANNUALE.md (storico, non in ordine cronologico).

## Settembre 2026
- **PWA clienti installabile — completata (build separata `/customers/`)**: doppia build nell'app operatori: la SPA di cassa/ordini cloud resta NON installabile e intoccata, mentre i clienti hanno una **PWA installabile** servita su **`/customers/`** (base `/customers/`, output **`dist-customers/`** gitignorata). ① **config**: `vite.customer.config.ts` (entry `customers.html`, base `/customers/`, manifest PWA con icone `pwa-64x64 | pwa-192x192 | pwa-512x512 | maskable-icon-512x512 | apple-touch-icon-180x180`, `registerType: 'prompt'` — banner aggiornamento manuale, offline = app-shell+asset statici, push = feature futura, **operatori NON installabili**); `vite-env.d.ts` con i reference `vite/client` + `vite-plugin-pwa/client`; script `build:customers` (`tsc -b` + `vite build --config vite.customer.config.ts`). ② **app-shell**: `CustomersPage`/`CustomerPwaPrompt` (modale "Installa app" con dismiss, varianti prompt/banner) con SCSS dedicato; `customer-router.tsx` (route pubbliche `/events/:eventId` dettaglio, `/events/:eventId/stands`, `/events/:eventId/menu`, slideshow, eventi, recensioni evento/stand, tracking `/track/:orderId`, track menu `.local`? no — solo cloud) con navbar e layout scuri navy; `customers.html` + `main-customers.tsx` entry. ③ **server duale** (`server.mjs`): serve `dist/` a `/` e `dist-customers/` a `/customers/` con `spaFile()` (fallback SPA + guard path-traversal), MIME completi (`.webmanifest`), e `customers.html` fallback per le route clienti. ④ **`render.yaml`**: `buildCommand` ora `npm ci --include=dev && npm run build && npm run build:customers` così Render builda anche i clienti; `frontend/.gitignore` aggiunta **`dist-customers/`** (fix .gitignore applicato e verificato in questa sessione). ⑤ **smoke test finale** (server duale, porta 4174): `/customers/` 200 text/html, `/customers/manifest.webmanifest` 200 application/manifest+json, `/customers/sw.js` + workbox 200, icone PWA 200, `/customers/menu/123` 200 (SPA fallback), `/` + `/manifest.webmanifest` (operatori) 200, `/orders/xyz` 200 (SPA fallback operatori) — tutto ✓. Verifica: build operatori (`tsc -b` + `vite build`) ✓, build customers ✓ (precache 26 entry), lint 0 errori, 43 test vitest ✓. Solo file cloud: **nessuna rigenerazione di `distro/local-app.tar` necessaria**.
- **Fix Master Cambio — totale carichi/rimborsi nella riga TOTALE**: nel report le casse singole mostravano correttamente "N carichi / M rimborsi", ma la riga TOTALE la scriveva sempre "0 carichi / 0 rimborsi" (i `sinceTopUpCount`/`sinceRefundCount` totali non erano calcolati dal backend né letti dal frontend). Fix: `totals` del report `/cash-registers/report` ora include **`sinceTopUpCount`/`sinceRefundCount`** (somma per tutte le casse), esposti anche nel tipo `CashRegistersReport`; il placeholder della riga TOTALE in `CashRegistersPage` li usa al posto degli zeri hardcoded. Test `integration-cash-registers.test.ts` rafforzato: dopo un top-up di 20 € (rate 2 → 40 crediti) e un refund di 5 crediti (real 2.5 €) il report totale atteso è `sinceTotalCount=2`, `sinceTopUpCount=1`, `sinceRefundCount=1`, `euroContent=17.5`, `creditsContent=-35`. Backend typecheck ✓, **437 test ✓**, lint 0 errori; frontend build (tsc+vite) ✓, 43 test vitest ✓, lint 0 errori (13 warning pre-esistenti). Solo file cloud: **nessuna rigenerazione di `distro/local-app.tar` necessaria**.
- **Fix sessione — recensioni, coupon e "Master Cambio"** (tutti i 7 punti della sezione "Fix di questa sessione" di TODO): ① **recensioni admin** — `getManageReviews`/`toAdminReview` restituiscono ora `eventName`/`standName` reali (il backend carica il nome evento con `.select('name')` e fa una mappa dei nomi stand via `StandModel.find({ _id: { $in: standIds } })`); `ReviewsManagePage` li usa (niente più "Evento" al posto del nome né standId nel nome dello stand). Test: asserzioni `eventName`/`standName` in `integration-reviews.test.ts`. ② **verifica coupon leggibile** — `.notice` di `CouponPanel.module.scss` ora è rosso scuro su sfondo rosso chiaro con bordo e peso 600 (prima quasi invisibile). ③ **link "Apri Cambio Valuta"** — puntava a una rotta inesistente `/events/:eventId/exchange` (404); ora apre `/admin/events/:eventId/exchange` in **nuova tab** (`target="_blank"` + `rel="noopener noreferrer"`). ④ **coupon valore/prodotto** — `redeemValue` (backend) accetta anche **`userId`** oltre a `eventUserId` (le due pagine cassa usano l'id utente della lista `/users`, non l'EventUser id; 404 con messaggio distinto se l'utente non è registrato all'evento); tipi di `redeemValuePromotion` corretti in `lib/promotions.ts` (era `amount`, ora `valueAmount` + `balance` + `transactionId`); `CouponPanel` riscatta il buono **inline** col pulsante "Riscatta sul portafoglio del cliente selezionato" (usa `customerUserId={selectedCustomerId}` passato dalle casse, conferma via `window.confirm`, box con importo accreditato + nuovo saldo) e per i coupon prodotto mostra il suggerimento "Aggiungi al carrello «prodotto» per applicare l'omaggio" quando non ancora applicati. Test: +2 in `integration-promotions.test.ts` (redeem by userId → saldo; utente non legato all'evento → 404). Suite backend 47 file / **437 test ✓**, typecheck ✓, lint 0 errori. ⑤ **chiusura cassa dal Master Cambio** — `CashRegistersPage` ha una nuova colonna "Azioni": per le casse **aperte** compare il pulsante "Chiudi" che apre una `ConfirmModal` danger ("Chiuderla? resterà come storico in sola lettura") → `POST /exchange/:eventId/cash-registers/:cashRegisterId/close` → reload del report; gli errori sono mostrati in un banner rosso `.error`. ⑥ **stampa Master Cambio (pagina bianca)** — eliminata la causa dominante: la **sidebar admin** (`AdminSidebar` è `position: fixed; height: 100vh; z-index: 40`) ora viene nascosta in `@media print` (`.admin aside { display: none !important }` in `global.scss`) e la **colonna Azioni** è schermata in stampa (`.printHide`) — senza il hide la sidebar fissata si sovrappone a ogni pagina coprendo lo specchio di stampa. Gotcha documentato in `docs/ARCHITECTURE.md` (sezione Printing). ⑦ **totale senza stato** — la riga TOTALE non mostra più il badge "Aperta" (cella Stato → "—"). Solo frontend per ②③⑤⑥⑦, backend per ①④; **nessun tocco a `.local/`: nessuna rigenerazione di `distro/local-app.tar` necessaria**.
- **Casse evento multiple (banco cambio) + page "Master Cambio"**: ① **backend** — nuovo modello `CashRegister` (collezione `cashregisters`: `eventId`, `name` max 80, `status open|closed` default open, `openedByUserId`, `openedAt`, `closedAt`, `cashFloat { euro, credits, setAt }`, timestamps, index `{ eventId, status, openedAt }`); transazioni e movimenti di cassa ora portano `cashRegisterId` (campi nuovi su `EventUserTransaction` e `CashRegisterMovement`, sempre nullable per retro-compatibilità). API `/api/exchange/:eventId/cash-registers` (exchange-admin / platform-admin): `GET` (lista con operatore di apertura), `POST` (apre con auto-nome `Cassa N`; **409 `code:'name_taken'`** se esiste già una cassa aperta → si può forzare con `{ force: true, cashRegisterToClose }` che chiude l'altra e riapre sulla macchina corrente, dopo conferma frontend), `GET /report` (`from`/`to` opzionali su `occurredAt`, default `from` = `event.startDate`; conteggi **solo top-up/refund**), `PATCH /:cashRegisterId` (rinomina, 409 se nome già in uso tra le aperte), `POST /:cashRegisterId/close` (404 se chiusa — idempotente solo con `status === 'open'`). **Validazione open a ogni operazione**: `topUp`/`refund`/`setCashFloat`/`addCashMovement` con `cashRegisterId` → 400 "Cassa chiusa, operazione non ammessa" se la cassa non è aperta. `setCashFloat` con `cashRegisterId` scrive su `CashRegister.cashFloat` (senza → legacy `Event.cashFloat`). Balance per cassa restituisce anche `topUpReal`/`refundReal`; chiusa = snapshot storico, nessuna scrittura. `resetEventOrders` elimina ora anche casse + movimenti. ② **frontend `EventExchangePage`** ("Cambio"): ogni postazione apre la propria cassa (nome proposto editabile, badge "Aperta", meta operatore+orario, Rinomina, **Chiudi cassa** con conferma); al load l'id della cassa aperta si ripristina via `localStorage` (`sfe_cash_register_<eventId>`); se a runtime il balance risponde `status!=='open'` (force-close da altra macchina) la pagina ricade su "Apri cassa" con avviso; **senza cassa aperta i form top-up/refund sono disabilitati e compare il banner `.cassaLocked`**; saldi/fondo/movimenti mostrati sono quelli **della cassa attiva** (`CashRegisterBalance`), fondi configurati per cassa. ③ **nuova pagina admin `CashRegistersPage`** ("Master Cambio", `/admin/events/:eventId/cash-registers`, sidebar Finanziario, exchange-admin/platform-admin): tabella di TUTTE le casse con fondo €/crediti, contenuto €/crediti, **conteggio transazioni da/versione `datetime-local`** (pure vacue → da `event.startDate`), riga TOTALE, auto-refresh 5s, pulsante Stampa (riusa i moduli di `EventReportPage`); il `title` è registrato in `SEGMENT_LABELS` e la route NON matcha la regex `isExchange` di `AdminLayout` (chrome/sidebar visibili). Test: nuovo `integration-cash-registers.test.ts` (9 test: apertura auto-nome + rinomina + 409 duplicato + force-close, transazioni legate alla cassa giusta, movimento legato a cassa, balance con fondi/contenuti e conteggi since, report aggregato con filtro e totali, chiusa → blocco operazioni 400, 401/403). Suite backend 47 file / **435 test OK**, typecheck ✓, lint 0 errori; frontend build (tsc+vite) ✓, 43 test vitest ✓, lint 0 errori (13 warning pre-esistenti). Solo file cloud: **nessuna rigenerazione di `distro/local-app.tar` necessaria**.
- **Pagina di tracking dedicata per lo stand (rimosso dalla cassa)**: ① il tracking è stato **rimosso dalla cassa** — `OrderTrackingModal variant="inline"` era montato dentro l'overlay "Ordine creato" di `CashierOrderPage`/`EventCashierPage`, dove però compare già la ricevuta; rimosso il componente e il suo import da entrambe le pagine (le broadcast `broadcastOrderCreated`/`broadcastTrackingClear` sulle casse restano invariate, così i modali standalone delle altre pagine/tab si chiudono comunque). ② nuova **pagina dedicata al tracking** per stand (`StandTrackingPage`, route standalone `events/:eventId/stands/:standId/tracking` — **niente navbar**, come `ordersqueue`): **banner** (`stand.coverImage`) full-width in alto e sotto **due colonne** — a sinistra il pannello identità **in verticale** (logo `logo ?? coverImage` in alto, poi **nome dello stand** e **nome della manifestazione** centrati — il **numero è omesso** perché già nel tracking a destra), a destra **solo il tracking abilitato automaticamente** (nuova variante **`page`** di `OrderTrackingModal`: card embedded senza overlay, nessun Escape; placeholder "Preparazione ordine in corso..." via `.empty`/`.emptyText`/`.emptyHint`, riutilizzati dall'empty state `inline` cancellato). **Tema evento** (`useEventTheme`, CSS vars `--color-brand/brand-deep/highlight`, fallback navy); sotto i 900px le colonne impilano verticalmente. ③ **comportamento "in attesa del prossimo"**: la pagina **NON mostra l'ultimo ordine all'apertura** — cattura il **`createdAt`** dell'ordine più recente come baseline e resta in attesa del PROSSIMO ordine. **Baseline per DATA di emissione, MAI per `orderNumber`** (il contatore può essere azzerato da `POST /api/orders/event/:eventId/reset`): `OrderTrackingModal` confronta `createdAt` in ms e il backend `getStandKioskRecent` ordina per **`createdAt: -1`** (era `orderNumber: -1`). Il **`tracking-clear`** della cassa (chiusura ricevuta) svuota la pagina e l'ordine mostrato non ricompare col polling (baseline + `clearedAtRef`, race-safe); se l'ordine esce dagli stati in-lavorazione la pagina torna vuota; si ripresenta solo al prossimo ordine. ④ il collegamento sta nella **console di admin** (`StandManagePage`, sezione Operazioni accanto a Cassa e Coda ordini): card "Tracking ordine" (🔍) che apre la pagina in nuova tab, gated da `selectedEventId && eventOngoing`. Verifica: backend (orders.controller.ts) typecheck ✓ + **426 test ✓** (46 file, +1 `kiosk-recent` dopo reset del contatore), lint 0 errori; frontend build (tsc+vite) ✓, 43 test vitest ✓, lint 0 errori (13 warning pre-esistenti). Solo file cloud: **nessuna rigenerazione di `distro/local-app.tar` necessaria**.
- **Fix chiusura tracking modal con ricevuta cassa**: race condition nel modale standalone: `broadcastOrderCreated` lanciava `load()` async; se la `fetch` di `kiosk-recent` risolveva **dopo** il `tracking-clear` emesso dalla cassa, `load()` ri-mostrava il modale appena nascosto. Fix: `load()` registra `startedAt` prima della fetch; `onTrackingClear` e `dismiss()` scrivono `clearedAtRef` (`Date.now()`); alla risoluzione, se `clearedAtRef > startedAt` il `setKiosk`/`setActive` sono saltati. Rimosso il pulsante `×` manuale (`.closeBtn`) dalla card standalone — la chiusura avviene solo tramite ricevuta cassa / overlay / Escape. Solo frontend (OrderTrackingModal + SCSS), build ✓, 43 test vitest ✓, lint 0 errori. Nessuna rigenerazione `distro/local-app.tar`.
- **Tracking ordini — restyling UX del modale (mostra SOLO nuovi ordini, toggle resta premuto, chiusura con la ricevuta cassa) + stand name/numero/logo nel track**: ① **baseline + solo nuovi ordini** — `OrderTrackingModal` (variante standalone) non mostra più l'ultimo ordine in lavorazione già esistente quando si attiva il toggle: cattura in una ref il `orderNumber` del più recente alla prima visualizzazione (`baseline`) e **appare solo per ordini NUOVI** (`orderNumber > baseline`), uno alla volta, al loro arrivo (push istantaneo/polling). ② **chiusura = solo dismiss** — X/overlay/Escape chiamano `dismiss()` che nasconde la visualizzazione corrente **senza disattivare il tracking**: il toggle resta premuto e il modale riappare al prossimo ordine (prima chiudere il modale spegneva il tracking). Rimossi tutti gli stati vuoti "Nessun ordine in attesa": standalone senza ordine attivo e con stand → `null`, senza stand → solo il dropdown stand; prop `onClose` eliminata da `OrderTrackingModalProps` e da tutti i call site. ③ **chiusura allineata alla ricevuta cassa** — nuovo evento `tracking-clear` (`broadcastTrackingClear` + `onTrackingClear`, registro locale + BroadcastChannel `sfe_tracking_orders`); le casse hanno rimosso `useTrackingEnabled('cashier', …)` (che nascondeva perennemente il modale inline) e ora il modale inline "Ordine creato" è sempre montato nell'overlay; alla chiusura della ricevuta emettono `tracking-clear` così **i modali standalone delle altre pagine/tab si chiudono** (il standalone fa `dismiss()`). ④ **stand nel track card** — `getStandKioskRecent` seleziona `name numbers logo coverImage` e restituisce **`standNumber`** (da `numbers` match per `eventId`) e **`standLogo`** (`logo ?? coverImage`); `KioskState` esteso e blocco identità `.standIdentity` (logo + badge numero + nome) nel card prima del conteggio coda. Test backend `orders.test.ts` aggiornati (kiosk-recent con `numbers`+`logo`, asserzioni `standNumber`/`standLogo`). Verifica: backend typecheck ✓ + 24 test orders ✓ + lint 0 errori; frontend build (tsc+vite) ✓ + lint 0 errori (13 warning pre-esistenti) + 43 test vitest ✓. La suite backend intera NON è stata rieseguita (solo `orders.test.ts`). Solo file cloud: **nessuna rigenerazione di `distro/local-app.tar` necessaria**.
- **Tracking ordini — toggle per tab (sessionStorage) + notifiche self da ogni sorgente**: ① **persistenza** — il toggle per-pagina passa da `localStorage` a **sessionStorage** (`isTrackingEnabled`/`setTrackingEnabled` in `lib/tracking.ts`): il tracking resta attivo su navigazione e F5 nella stessa tab e si spegne solo alla **disattivazione o chiusura di tab/browser** (prima con localStorage sopravviveva alla chiusura del browser). Rimosso il listener cross-tab `storage` da `useTrackingEnabled`: ogni tab è indipendente (niente più sync del toggle tra finestre). ② **notifiche con self-delivery** — `broadcastOrderCreated` notifica sia un registro locale (`localListeners`, stessa tab) sia il BroadcastChannel `sfe_tracking_orders` (altre tab); `onOrderCreated` sottoscrive entrambi con unsubscribe combinato, così **anche la tab che crea l'ordine aggiorna subito il modale** (BroadcastChannel non si autoconsegna). ③ **tutte le sorgenti** — `broadcastOrderCreated` aggiunto dopo il `createOrder` in `EventStandMenuPage` (ordini dal menu pubblico), oltre alle casse `EventCashierPage`/`CashierOrderPage`. Il modale resta montato in automatico finché il tracking non viene disattivato (`OrderTrackingModal`, varianti standalone/inline). Verifica: frontend build (tsc+vite) ✓, lint 0 errori (13 warning pre-esistenti), 43 test vitest ✓; backend non toccato. Solo file cloud: **nessuna rigenerazione di `distro/local-app.tar` necessaria**.
- **Tracking ordini — toggle per pagina + push istantaneo + pulsante sulle pagine pubbliche**: ① **toggle per pagina** — `useTrackingEnabled(page, eventId)` in `lib/tracking.ts` con chiavi localStorage distinte `sfe_tracking_admin_<eventId>` / `sfe_tracking_slideshow_<eventId>` / `sfe_tracking_cashier_<eventId>` / `sfe_tracking_public_<eventId>`, sincronizzate fra tab via listener `storage` (via il master switch unico). ② **push istantaneo** — le casse (`EventCashierPage`/`CashierOrderPage`) chiamano `broadcastOrderCreated({ eventId, standId, orderId, orderNumber })` subito dopo la creazione dell'ordine; `OrderTrackingModal` si sottoscrive con `onOrderCreated` (BroadcastChannel `sfe_tracking_orders`; lo **stesso tab non riceve i propri messaggi**) e ricarica subito `kiosk-recent` se evento/stand combaciano — il **polling 5s resta come fallback** (es. schermi su macchine diverse). ③ **pulsante mirino sulle pagine pubbliche** — `PublicHeader` lo mostra su TUTTE le route pubbliche con `eventId` (skip `hideChrome`), visibile SOLO agli utenti con ruoli `event-admin`/`stand-admin`/`platform-admin` (check `/auth/me/roles` in `PublicLayout`, non chiamato per utenti anonimi); il modale standalone usa il **dropdown stand**. ④ **icona mirino al posto del testo "Tracking"** in slideshow, sidebar admin (solo icona) e navbar admin. Verifica: frontend build (tsc+vite) ✓, lint 0 errori (13 warning pre-esistenti), 43 test vitest ✓; backend non toccato. Solo file cloud: **nessuna rigenerazione di `distro/local-app.tar` necessaria**.
- **Chiosco QR ordini + pagina track ordine con notifica "pronto"**: ① **backend** — due nuovi endpoint pubblici (registrati PRIMA di `authMiddleware` in `orders.routes.ts`, accanto a `/orders/:orderId/receipt` e `/orders/stand/:standId/ordersqueue`): `GET /api/orders/:orderId/track` (`getOrderTrack`: minimi dati `{ id, orderNumber, status, isGift, readyAt, createdAt, eventId, standId, eventName, standName, items[{ productName, quantity, stationName }] }` — niente dati cliente né prezzi, valore anche per ordini già completati/cancellati) e `GET /api/orders/stand/:standId/kiosk-recent` (`getStandKioskRecent`: filtra `confirmed/preparing/ready`, opzione `eventId`, restituisce l'ultimo ordine per `orderNumber: -1`, `queueCount`, `trackUrl` costruita da `req.query.url` → fallback origin/request e **QR data URL server-side** con la lib `qrcode` — il frontend non ha lib QR). ② **frontend** — pagina pubblica **`TrackOrderPage`** (`/track/:orderId`, fullscreen, polling 5s): schermata "PRONTO!" verde quando l'ordine passa a `ready` con **beep Web Audio** (2 doppi toni) + **Notification API opzionale** (pulsante "Abilita notifiche", permesso richiesto solo su click), stati completato/annullato/in lavorazione, riepilogo "Cosa hai ordinato"; pagina pubblica **`TrackOrderPage`** (`/track/:orderId`, fullscreen, polling 5s): schermata "PRONTO!" verde quando l'ordine passa a `ready` con **beep Web Audio** (2 doppi toni) + **Notification API opzionale** (pulsante "Abilita notifiche", permesso richiesto solo su click), stati completato/annullato/in lavorazione, riepilogo "Cosa hai ordinato"; modale riutilizzabile **`OrderTrackingModal`** (refactor dell'ex pagina chiosco, **rimossa** — niente più `/kiosk`): varianti **`standalone`** (overlay fisso centrato, tema scuro navy, z-index 200) montata globalmente in `AdminLayout` (skip sulle route cassa) e in `SlideshowPage` via pulsante "Tracking" in testata, e **`inline`** (card senza backdrop) dentro l'overlay "Ordine creato" delle due pagine cassa (`EventCashierPage`/`CashierOrderPage`, `stopPropagation`, chiusa insieme al riepilogo). Master switch per-browser `localStorage['sfe_tracking_enabled_<eventId>']` sincronizzato fra tab (hook `useTrackingEnabled` in `lib/tracking.ts` + listener `storage`), voce **"Abilita tracking"** — stato ON/OFF — nella nav di `AdminSidebar`. Se la pagina non fissa uno stand, il modale mostra un **dropdown stand** (fetch `/stands?eventId=`); il polling 5s (`fetchStandKioskRecent` con `url = window.location.origin`) mostra **QR grande + numero ordine + stato + riepilogo + badge "N ordini in coda"** per l'ultimo ordine in lavorazione. Helper + tipi in `lib/orders.ts` (`fetchOrderTrack`, `fetchStandKioskRecent`, `OrderTrack`, `KioskState`; rimosso `KIOSK_ACTIVE_KEY`). `PublicLayout` tiene `hideChrome` solo per `/track` e `/ordersqueue`. Test: +4 in `orders.test.ts` (track pubblico minimale senza dati cliente/prezzi; 400/404 track; kiosk-recent ultimo in lavorazione + QR + filter evento; kiosk vuoto + 404/400), suite backend 46 file / **425 test ✓**, typecheck ✓, lint 0 errori; frontend build (tsc+vite) ✓, 43 test vitest ✓, lint 0 errori (13 warning pre-esistenti). TODO.md punto 2 → fatto. Solo file cloud: **nessuna rigenerazione di `distro/local-app.tar` necessaria**.
- **Recensioni — gate acquisto rimosso + campo "Cosa hai comprato" + % affidabilità**: ① **backend**: `createReview` non richiede più un ordine non cancellato (rimossa la guardia `OrderModel.exists` → 403 end-to-end per evento e stand: ora un utente registrato può recensire qualsiasi evento/stand); nuovo campo `Review.whatBought` (String, trim, default null, max 200, sanitizzato server-side) incluso in `toPublicReview` e nel `create`; `getReviewsSummary` espone `registeredCount` nelle aggregazioni evento e per-stand. ② **frontend**: `ReviewForm` mostra il campo "Cosa hai comprato" (testo libero facoltativo, max 200 caratteri) solo per le recensioni stand e il testo "Verificheremo il tuo acquisto…" è stato rimosso; badge "Acquisto verificato" → **"Utente registrato"** (`ReviewCard` e `ReviewsManagePage`, il campo `isVerified` resta = `!!userId` ma il significato è "utente registrato"); `ReviewCard`/`ReviewsManagePage` mostrano la riga "Comprato: …"; `EventReviewPage`/`StandReviewPage` mostrano "X% da utenti registrati" accanto al conteggio (percentuale calcolata client-side da `registeredCount`, helper `registeredPercent` in `lib/reviews.ts`). Aggiornate `docs/TODO.md` (punto 1 segnato come fatto; punto 2 chiosco riscritto conciso). Test: adattati i 2 test del gate (ora 201) + +1 per `whatBought` (round-trip sanitizzato + max 200) + `registeredCount` nel summary, suite backend 46 file / **421 test ✓**, typecheck ✓, lint 0 errori; frontend build (tsc+vite) ✓, 43 test vitest ✓, lint 0 errori (13 warning pre-esistenti). Solo file cloud: **nessuna rigenerazione di `distro/local-app.tar` necessaria**.
- **Data limite adesione stand**: nuovo campo `Event.adhesionDeadline` (Date, default null) configurabile in admin in `EventsPage` ("Termine adesione stand", `type="date"`, vuoto = nessun limite) ed esposto in `toEventResponse` (create/update/read round-trip). `duplicateEvent` NON lo copia nella nuova edizione (resta null, come le altre scadenze). **Gate backend** in `stand-adhesions.controller.ts`: helper `adhesionDeadlineError(deadline)` → 400 con `'Il termine per la presentazione delle adesioni è scaduto.'` in `createAdhesion` (dopo il gate `regulationDocument`), `updateAdhesion` (prima dei controlli approved) e `submitAdhesion` (prima di `completenessErrors`); `submitAdhesion` carica l'evento con `.select('adhesionDeadline participationFee deposit')`. **Blocco wizard** (`StandAdhesionWizardPage`): `EventRef` include `adhesionDeadline`, il timestamp è catturato una volta via `useState(() => Date.now())` (regola React impure) e se la data è scaduta E (nessuna adesione OPPURE adesione editabile draft/rejected/integration) la pagina mostra un avviso `deadlineNotice` ("Il termine per la presentazione delle adesioni era il <data>…") senza form; adesioni in submitted/approved restano consultabili. Test: +3 in `integration-stand-adhesions.test.ts` (round-trip evento; create 400 con deadline passata; update+submit 400 dopo spostamento al passato), suite backend 46 file / **420 test ✓**, typecheck ✓, lint 0 errori; frontend build (tsc+vite) ✓, 43 test vitest ✓, lint 0 errori (13 warning pre-esistenti). Solo file cloud: **nessuna rigenerazione di `distro/local-app.tar` necessaria**.
- **Recensioni — QR di TUTTI gli stand in un'unica pagina stampabile + fix layout pagine recensione**: ① **backend**: nuovo endpoint `GET /api/events/:eventId/reviews/qrcodes/all` (event-admin / platform-admin) — `getAllReviewQrCodes` in `reviews.controller.ts` carica evento + stand (`.select('_id name numbers')`, `.lean()`), li ordina per **numero stand evento** (stand senza numero in fondo, poi per nome) e genera un QR data URL per la pagina recensione di ciascuno → `{ items: [{ standId, standName, number, url, qrCode }] }`. Route registrata PRIMA di `/:reviewId` in `reviews.routes.ts` (sezione protetta da `authMiddleware` + `hasRole`). ② **frontend**: nuova pagina admin `ReviewsQrCodesPage` (`/admin/events/:eventId/reviews/qrcodes`) con guardia roles, header (nome evento) + pulsante Stampa, griglia di card (nome stand + badge numero + QR + "Recensione dello stand"), CSS `@media print` = griglia a 3 colonne con toolbar nascosta (classe `no-print`); link "QR recensioni di tutti gli stand" nella toolbar di `ReviewsManagePage`. ③ **fix layout**: `EventReviewPage` e `StandReviewPage` renderizzavano a larghezza piena (senza `page-shell`) — contenuto avvolto in `<div className="page-shell">` come le altre pagine pubbliche. Test: +2 in `integration-reviews.test.ts` (lista completa QR per event-admin; 401/403), suite backend 46 file / **417 test ✓**, typecheck ✓, lint 0 errori; frontend build (tsc+vite) ✓, 43 test vitest ✓, lint 0 errori (13 warning pre-esistenti). Solo file cloud: **nessuna rigenerazione di `distro/local-app.tar` necessaria**.
- **Feedback & Recensioni (evento e stand)** — ① **backend**: nuovo modello `Review` (`eventId`, `standId` null = recensione evento, `rating` 1-5, `comment` ≤1000, `reviewerName`, `reviewerEmail` privata, `userId`/`guestTokenHash` nullable con **indici unici parziali** — un utente o un guest può recensire al massimo UNA volta per target; `status: visible|hidden` per la moderazione *post-hoc*). Nuova API `/api/events/:eventId/reviews` (router `mergeParams`): `GET /` (lista pubblica, `standId` opzionale), `GET /summary` (media evento + per-stand), `GET /qrcode?standId=` (data URL), `GET /my` (mie recensioni), `POST /` — composta da `getEventReviews`, `getReviewsSummary`, `getMyReviews`, `createReview`, `getManageReviews`, `updateReviewStatus`, `deleteReview`, `getReviewQrCode` in `reviews.controller.ts`. **Gating**: utenti registrati devono aver fatto un **ordine non cancellato** per evento/stand (`OrderModel.exists`) → altrimenti 403; ospiti anonimi devono fornire `reviewerName` e ricevono un **guest token** salvato solo come sha256 (`guestTokenHash`), rimandato come header `x-access-token`; secondi tentativi → **409**. Moderazione **post-hoc**: le recensioni sono visibili subito, l'admin (event-admin / platform-admin) può nasconderle/mostrarle/eliminarle. ② **frontend**: pagine pubbliche `EventReviewPage` (`/events/:eventId/review`) e `StandReviewPage` (`/events/:eventId/stands/:standId/review`) con hero + media + form (`ReviewForm` con stelle `RatingStars` e campi anonimi) + lista (`ReviewCard` con badge "Acquisto verificato"/"Nascosta"); pagina admin `ReviewsManagePage` (`/admin/events/:eventId/reviews`, voce **Recensioni** nella sidebar Gestione) con guardia `/auth/me/roles`, filtri per stand/stato, paginazione e **pulsanti QR** per la pagina recensione evento e dello stand selezionato; **media inline** sul menu stand (`EventStandMenuPage`: link "★ media (n)" nella header) e sul dettaglio evento (`EventDetailPage`: link "Recensioni (n)" nelle hero actions + media per stand nelle card); **QR recensioni** in `StandManagePage`. Client `frontend/src/lib/reviews.ts` (guest token in `localStorage['reviewGuestToken:<eventId>']`). Test: `integration-reviews.test.ts` **19 test** (CRUD, gating verificato/anonimo, 409, summary, manage, hide/unhide/delete, QR) → suite backend 46 file / **415 test ✓**, typecheck ✓, lint 0; frontend build (tsc+vite) ✓, 43 test vitest ✓, lint 0 errori (13 warning pre-esistenti). Solo file cloud: **nessuna rigenerazione di `distro/local-app.tar` necessaria**.
- **Fix menu operativo stand (platform-admin) + data inizio nel dropdown evento**: ① **menu "Operativo" della sidebar**: `GET /api/auth/me/stands` ora include **TUTTI gli stand per i platform-admin** (prima restituiva solo gli stand collegati ai ruoli espliciti stand/evento dell'utente → un platform-admin con ruolo platform ma senza ruoli legati a uno stand di "Evento di prova" non vedeva "Stand di Prova" nel menu Operativo). Stesso endpoint usato da Cassa/Ordini/Ricevuta per l'autorizzazione → corregge anche i falsi "forbidden" sui pagine stand per i platform-admin. ② **dropdown "Evento attivo"**: ogni opzione mostra anche la **data di inizio** (`Nome — gg/mm/aaaa`) così le edizioni con lo stesso nome sono distinguibili. Test: +2 in `auth.test.ts` (platform-admin vede tutti gli stand+postazioni; utente regolare senza ruoli no), suite backend **396 test ✓**, typecheck ✓, lint 0 errori; frontend tsc ✓, lint ✓ (13 warning pre-esistenti). Solo file cloud: **nessuna rigenerazione di `distro/local-app.tar` necessaria**.
- **Adesione stand — commissioni senza tetto accettate nel wizard + fix testi + dimensioni upload**: ① **fascia commissione "senza tetto"**: una fascia con importo lordo massimo vuoto/0 € rappresenta gli **incassi non compresi nelle altre fasce** (oltre l'ultimo tetto) — consentita **al massimo una** fascia di questo tipo (validazione in `handleSubmit` di `EventsPage`); nel modulo di adesione generato (`adhesion-form.service.ts`) la fascia residuale è resa a parte "oltre l'ultimo tetto (non compresi nelle altre fasce)" con nota esplicativa; il calcolo fee in `StandSettlementsPage` (`resolveFee`) usa la banda residuale come fallback quando nessuna fascia con tetto copre l'importo. ② **accettazione fee nel wizard web**: nuova sezione "④bis Commissioni sugli incassi" con elenco fasce e checkbox `feesAccepted` — campo nuovo sul modello `StandAdhesion` (default false); il submit richiede l'accettazione quando l'evento ha `feeBands` (backend `completenessErrors`), mostrata anche in `AdhesionsManagePage` ("Commissioni sugli incassi: accettate/non accettate"). ③ **fix testo**: il wizard mostrava letterale `dall\u2019organizzazione` (escaping in testo JSX → `dall&apos;organizzazione`) nel prerequisito referente. ④ **dimensioni upload**: hint "1080 x 220 px" (banner stand) e "quadrato, minimo 512 x 512 px" (logo) sotto gli `ImageUploader` del wizard. Documentazione `docs/ADESIONE_STAND.md` Sezione F aggiornata. Test: +2 backend (fees accettazione obbligatoria, fascia senza tetto nel modulo generato), suite **394 test OK**, typecheck ✓, lint 0 errori (13 warning pre-esistenti), frontend build (tsc+vite) ✓ + 43 test vitest ✓. Solo file cloud: nessuna rigenerazione di `distro/local-app.tar` necessaria.
- **App locale — layout fullscreen + tema evento e code in ordine FIFO**: ① **ordine delle code** (coda postazione, coda pubblica/display — cloud e app locale): gli elementi sono ordinati per **`orderNumber` crescente** (il primo ordine in testa, gli altri a scendere) tramite sort lato frontend — la coda postazione prima mostrava il più recente in alto (il backend `listOrders` ordina per `createdAt: -1`); nessuna modifica al backend, evitando effetti su altre pagine. ② **app locale** (`.local/`): `App.tsx` ora ha una **navbar** (brand "Street Food — Locale", tab a pillole Cassa/Coda Postazioni/Display Pubblico/Sync con badge ordini pendenti, nome evento a destra) e layout `100dvh` flex column con `main` fullscreen; `Cassa`, `CodaPostazioni` e `CodaPubblica` sono **a tutto schermo** con tema scuro che riprende quello del cashier remoto (`#1a1a2e`/`#16213e`/`#0f3460`, accento `#e94560`, verde `#28a745`); `CodaPubblica` replicata dal `StandDisplayPage` remoto (`#0f172a`/`#1e293b`/`#334155`, segnaposto "Pronto" verde). ③ **tema evento negli accenti**: `getMeta()` locale espone ora `theme` (brand/text/surface/highlight) letto dall'`EventModel` corrente, e `MetaContext` applica le CSS variables `--sf-brand`/`--sf-highlight` su `:root` — gli accenti locali (tab attiva, brand navbar, badge ordine, titoli sezione, pulsanti, bordo "in preparazione", badge omaggio) usano `var(--sf-brand)`/`var(--sf-highlight)` con **fallback ai default navy** quando l'evento non ha tema; sfondi scuri fissi invariati. Verifica: build `.local/frontend` ✓, `tsc --noEmit` backend locale ✓, frontend cloud build (tsc+vite) ✓, lint selettivo ✓. **`.local/` modificato: `distro/local-app.tar` rigenerata**.
- **Advertisement e slideshow — rifiniture UI**: dashboard admin con **card a griglia** (flex-wrap orizzontale) per ogni advertisement — immagine portrait grande in alto con badge stato sovrapposto, sotto nome, contatore apparizioni, campo Peso e pulsanti Attiva/Disattiva + Elimina dedicati. Pannello slideshow: **rimossa la barra superiore** — resta solo il pulsante di chiusura come **circoletto bianco con la X** (assoluto, in alto a destra sopra l'immagine); **titolo slideshow allineato a sinistra** nell'header (refresh + velocità restano a destra). Solo frontend, nessun tocco al backend: nessuna rigenerazione di `distro/local-app.tar` necessaria.
- **Advertisement — contatori apparizioni, titolo slideshow e keep-alive**: nuovo campo persistito `appearances` (default 0) sul modello `Advertisement`; endpoint pubblico `POST /api/advertisements/:advertisementId/appearance` (incremento `$inc`, nessuna auth) chiamato dalla slideshow ogni volta che mostra un advertisement (pannello aperto) e che alimenta il **totale apparizioni mostrato in admin accanto alla thumbnail** di ogni card. Nuovo endpoint `POST /api/advertisements/reset-appearances` (platform-admin / photo-admin) per **azzerare tutti i contatori**, con pulsante "Azzera contatori apparizioni" in `AdvertisementsPage` (conferma). Il pannello slideshow ora **NON ha più il titolo "Advertisement"** né il contatore "n app" (solo l'icona × per chiuderlo); è stato ripristinato invece il **titolo della slideshow con sottotitolo** nell'header della schermata (titolo editabile + nome evento, accanto ai controlli aggiorna/velocità). **`useKeepAlive` spostato dalle due pagine cassa** (`CashierOrderPage`/`EventCashierPage`) **alla slideshow** (`SlideshowPage`) — il ping anti-sleep/anti-idle ora tiene attivo il server/frontend durante la proiezione. Test `integration-advertisements.test.ts`: +4 (appearance incrementa, 400/404 su id invalidi, reset come platform-admin, 401 senza auth), suite backend 45 file / **392 test OK**, typecheck ✓, lint ✓; frontend build (tsc+vite) ✓, lint 0 errori (13 warning pre-esistenti). Solo file cloud: nessuna rigenerazione di `distro/local-app.tar` necessaria.
- **Advertisement — peso e selezione casuale ponderata**: nuovo campo `weight` (numero, min 1, default 1) sul modello `Advertisement`, gestito da API (`POST`/`PATCH`, clamp a minimum 1) e dalla pagina admin (input numerico **accanto alla thumbnail** nella lista + campo nel form di creazione). Il pannello slideshow ora **non ruota più in sequenza**: ogni rotazione sceglie un advertisement **in modo casuale ponderato sul peso** (`weightedPickIndex`, probabilità proporzionale a `weight`, evita ripetuto consecutivo se ci sono alternative) e mostra il **conteggio delle apparizioni** ("n app") della figura corrente al posto del progressivo "n / N". Inoltre **rimosso il titolo della schermata** (logo, nome evento e titolo slideshow editabile spariscono dalla barra superiore — restano solo aggiorna + pulsanti velocità). Test `integration-advertisements.test.ts`: +3 sui pesi (peso custom alla creazione, clamp a 1 su 0, update via PATCH); suite backend 45 file / **388 test OK**, typecheck ✓, lint ✓; frontend build (tsc+vite) ✓, lint 0 errori (13 warning pre-esistenti). Solo file cloud: nessuna rigenerazione di `distro/local-app.tar` necessaria.
- **Advertisement dedicato per lo slideshow**: nuovo modello trasversale `Advertisement` (non legato a un evento, collezione `advertisements`, campi `image`/`enabled`/`name`) con API `/api/advertisements`: `GET /` pubblico (solo `enabled: true`, per lo slideshow), `GET /manage` (platform-admin / photo-admin, tutte), `POST` (multipart `image` + nome opzionale, guard `platform-admin`/`photo-admin`), `PATCH /:advertisementId` (toggle `enabled`/nome) e `DELETE`. **SlideshowPage**: nuovo **pannello collassabile "Advertisement"** a destra — chiuso è un tab verticale sottile, aperto occupa `min(30vw, 100vh*0.7071)` (proporzione portrait A4) per tutta l'altezza della finestra, riducendo la griglia foto; mostra **un advertisement alla volta** che ruota con **la stessa temporizzazione dei pulsanti velocità** (`rotateSec`) e contatore "n/m"; i cambi immagine hanno una dissolvenza (`adFade`). Gestione admin in `AdvertisementsPage` (`/admin/advertisements`, voce in sidebar Foto): upload, attiva/disattiva, elimina. Test `integration-advertisements.test.ts` (8 test); suite backend 45 file / **385 test OK**, typecheck ✓, lint ✓; frontend build (tsc+vite) ✓, lint 0 errori (13 warning pre-esistenti). Nessun tocco a `.local/`: nessuna rigenerazione di `distro/local-app.tar` necessaria.
- **Pulizia lint pre-esistenti → 0 errori** su entrambi i package (erano 33 backend + 58 frontend, nessuno introdotto da feature recenti). Frontend: disattivate di proposito in `eslint.config.js` le regole `react-hooks/set-state-in-effect` (39 errori: il pattern repo `void load()` con `setLoading(true)` sincrono; assente con react-hooks ≤6, nessun refactor) e `react-refresh/only-export-components` (7: helper condivisi esportati da `CurrencyDisplay`/`CategorySelect` e hook da `auth-context`/`ThemeProvider`); fix banali su `no-empty` ×9 (`catch { /* noop */ }`, tutti catch intenzionali), `no-useless-assignment` (`PhotoBoothModal`), `no-require-imports` (`vite.config.ts` convertito a import ESM `node:fs` + `new URL(…, import.meta.url)` al posto di `require`/`__dirname`, fallback SPA invariata). Backend: rimosso import morto `hashActivationToken` da `users.controller.ts` e sistemati **32 unused vars nei file di test** (import morti rimossi, destructure ridotte alle sole variabili usate). Restano solo 13 warning `exhaustive-deps` pre-esistenti (non toccati). Verifica: frontend build ✓ + 43 test vitest ✓ + lint 0 errori; backend typecheck ✓ + 372 test ✓ + lint 0 errori. Solo file `docs/` + sorgenti cloud: nessuna rigenerazione di `distro/local-app.tar` necessaria.
- App locale offline + sync remoto (primo step): il backend cloud espone le API di sincronizzazione `/api/sync` protette da bearer token statico (`SYNC_API_TOKEN` env) — `GET /events`, `GET /events/:eventId/stands`, `GET /events/:eventId/stands/:standId` (snapshot completo evento+stand: event, stand, stations, products, eventProducts, eventUsers, counter) e `POST /push` (upsert di ordini/transazioni/contatori/saldi event-user con guardia LWW). L'app locale (`.local/`) ha un pannello **Sync** che permette di selezionare evento e stand remoto e **importarli** sostituendo completamente i dati locali (wipe transazionale + insert che preserva gli `_id`); se esistono modifiche locali non sincronizzate il pannello chiede di **pushear prima** (modale di conferma). Ledger locale `SyncLedger` traccia le operazioni pendenti, `LocalState` conserva evento/stand attivo correnti; il frontend locale usa `MetaContext` (`Cassa`, `CodaPostazioni`, `CodaPubblica`) al posto della `config.ts` hardcoded. Auth server-to-server a token statico condiviso (`remoteUrl`/`remoteToken` in `.local/backend/src/config.ts`). Test in `backend/src/__tests__/controllers/sync.test.ts` (6 test).
- Media locale offline: durante l'import le immagini di evento/stand/prodotto vengono scaricate dal Cloudinary remoto e ripuntate all'endpoint statico locale (`/assets/*`, cartella `MEDIA_DIR` default `.local-assets` già gitignorata) così il menu/mappa funziona senza connessione. `media.service.ts` localizza i campi immagine (`Event.coverImage/logo/currencySymbol/gallery`, `Stand.coverImage/logo/gallery`, `Product.coverImage/gallery`) con filename sha1 del contenuto (idempotente, cache persistente; su errore di download resta l'URL remoto). Le immagini locali non vengono MAI pushato al cloud (`cleanForPush` scarta i subdoc immagine locali) e restano su disco come cache dopo il sync.
- App locale — immagini nella cassa: `GET /api/catalog/stand/:standId/menu` ora espone anche `coverImage` per ogni voce di menu, `coverImage` dello stand, `eventName`/`currencyName`/`currencySymbol` dell'evento; `Cassa` mostra la cover dello stand in testata, la thumbnail cover di ogni prodotto e il nome della moneta accanto ai prezzi e al totale. Porta unificata su **4000** (backend default + proxy vite, già in linea col docker-compose) e nuova doc operativa in `.local/README.md` (`REMOTE_URL`, `REMOTE_TOKEN`, `MEDIA_DIR`, `ASSETS_URL_PREFIX`, flusso sync).
- App locale — deploy unico e setup di sviluppo: `npm run local:up` (dalla cartella `.local/`) esegue `docker compose up --build` per un deploy tutto-in-uno (MongoDB replica set + seed + API + UI statica su `http://localhost:4000`); le variabili di deploy si impostano copiando `.local/.env.example` → `.env` (`PORT`, `MACHINE_ID`, `REMOTE_URL`, `REMOTE_TOKEN`, `SEED`, `ASSETS_URL_PREFIX`). `npm run setup:local` avvia per lo sviluppo un MongoDB dedicato via `docker-compose.db.yml` (replica set `rs0`), fa il seed e lancia backend (:4000) + frontend vite (:5173). Default `REMOTE_URL` = `https://streetfoodevents-api.onrender.com/api` applicato in `config.ts`, docker-compose e `.env.example`.
- App locale — distribuzione su notebook con immagine pronta: cartella **`distro/`** (gitignorata) contiene l'immagine Docker esportata (`docker save local-app -o distro/local-app.tar`) per installare l'app su un altro PC con solo Docker Engine (`docker load` + `docker run` con `-v mongo-data` e `-v local-media`; i volumi partono vuoti, il primo avvio fa il seed e dal pannello Sync si importano evento/stand dal cloud). **Regola operativa**: ad OGNI modifica ai sorgenti dell'app locale (o `Dockerfile`/`entrypoint.sh`/compose) va rieseguito `npm run local:up` e poi `docker save` per riaggiornare `distro/local-app.tar` — l'immagine non deve restare indietro rispetto al codice (vedi `.local/README.md`).
- Saldo utente nella pagina evento pubblica: `GET /api/events/:eventId` ora accetta l'autenticazione opzionale (`optionalAuthMiddleware`) e, se l'utente è loggato, restituisce anche `wallet: { balance } | null` (saldo crediti dell'utente per quell'evento). `EventDetailPage` mostra accanto al badge moneta un badge "saldo" con l'ammontare (o 0) nella moneta dell'evento per l'utente autenticato; non viene mostrato ai visitatori anonimi. Backend typecheck + 303 test OK, frontend build OK.
- Google Tag Manager con Consent Mode v2: il tag GTM viene caricato da `VITE_GTM_ID` (vuoto → analytics disattivo). Conformità GDPR: consenso analytics/ads parte `denied` di default, viene aggiornato via `updateConsent` al momento della scelta nel banner (Accept/Reject). Eventi custom sul dataLayer: creazione ordine menu (`sfe_order_created`), cambio valuta top-up/refund (`sfe_currency_exchange`), scansione POI contest (`sfe_poi_scanned`), invio foto via email (`sfe_photos_email_sent`). Contestualizzazione utente/evento/stand nel dataLayer (`context_set` con `user_role`, `analytics_event_id`, `analytics_stand_id`) in `PublicLayout`. Rimosso l'ID GA4 segnaposto da `analytics.ts` (i tag ora si configurano in GTM). Configurato container `GTM-W39HB9Q9` (in `.env` locale e `render.yaml`) + proprietà GA4 `G-94RMSM6R5Z`; guida tag in `docs/GTM_EVENTI.md`.
- Esporta per social dal menu stand (cloud, solo frontend): pulsante "Esporta social" nella header della pagina menu pubblico dello stand (`/events/:eventId/stands/:standId`), visibile solo a utenti autorizzati — platform admin, admin evento (`adminEventIds`) o admin dello stand (ruolo stand-scope via `GET /auth/me/stands`). Apre un modale (`SocialMenuExport`) che genera e scarica un **poster PNG 1080x1350** (4:5, formato social) con: logo della manifestazione + nome, **banner (`coverImage`) dello stand** come fascia sotto la testata (se presente) con il **logo dello stand** sovrapposto (fallback alla cover, o iniziale del nome), nome + slogan, **i prodotti in vendita con la foto thumbnail di copertina** (placeholder con iniziale quando manca, max 8 nel poster), luogo/date dell'evento e **QR code verso il menu dello stand** (dalla scan URL condivisa `GET /api/stands/:standId/qrcode`); i colori seguono il tema dell'evento. Nel modale un toggle **"Includi i prezzi"** (persistito in `localStorage`) abilita/disabilita i prezzi sia sul poster sia nel testo: con i prezzi attivi ogni prodotto mostra l'importo accanto alla foto nella moneta dell'evento, rappresentata dal **logo della moneta** (icona caricata sull'evento) oppure, in mancanza, dall'**iniziale in un circoletto** (`Ⓣ`); **il nome completo della moneta appare solo nella riga "Moneta evento: NOME (1 NOME = €)"** (inclusa come nota nel modale), mai accanto ai prezzi — valuta euro/simbolo semplice (<tt>€</tt>) mostrata come simbolo. Il **nome dell'evento** (tagline ripresa dalla descrizione breve) nel testo del post viene **ripulito dai tag HTML** (`stripHtml`), così la caption resta leggibile sui social. Senza prezzi il menu resta pulito come prima. Insieme al poster il modale offre il **testo del post** (caption pronta con nome stand, evento, data, luogo e lista prodotti — con prezzi e riga moneta se abilitati) con copia negli appunti e download `.txt`, più il QR del menu scaricabile separatamente. Generazione poster 100% client-side (canvas, immagini `crossOrigin`), nessun cambiamento backend. Helpers puri in `frontend/src/lib/socialMenu.ts` (`formatEventDateRange`, `buildSocialMenuCaption`, `formatCredits`, `formatMonetaLine`, `formatCurrencyInline`, `circledInitialText`, `stripHtml`, `isEuroCurrency`) con 25 test frontend; typecheck, build e vitest verdi. Nessun tocco a `.local/`: nessuna rigenerazione di `distro/local-app.tar` necessaria.
- Regolamento della manifestazione sull'evento (PDF opzionale): nuovo campo `Event.regulationDocument` (subdocumento `document.schema` — url, publicId, format, bytes, originalName; default null) caricabile dal form di creazione/modifica evento in `EventsPage`. Upload tramite nuovo endpoint autenticato `POST /api/upload/document` (`multer` dedicato, solo `application/pdf`, max 20 MB) che salva su Cloudinary come `resource_type: 'raw'` nella stessa cartella dell'evento, e `DELETE /api/upload/document` per la rimozione (destroy con `resource_type: 'raw'`). Il documento è esposto in tutte le response degli eventi (`toEventResponse`). Nella **pagina pubblica dell'evento** (`EventDetailPage`) il pulsante **"Scarica Regolamento"** nella barra azioni della hero appare solo se il documento è presente e scarica il PDF con nome **`regolamento-<manifestazione>-<anno>.pdf`** (nome evento normalizzato + anno di inizio; il PDF è scaricato come blob client-side per forzare il nome — un link cross-origin su Cloudinary ignora l'attributo `download` — con fallback all'apertura in nuova tab). Frontend: tipo `UploadedDocument` + `uploadDocument`/`deleteDocument` in `lib/upload.ts`, componente `DocumentUploader` (drag&drop, card con nome file/dimensione, link "Apri", rimozione). Nessun tocco a `.local/`: nessuna rigenerazione di `distro/local-app.tar` necessaria.
- Documentazione operativa — energia elettrica: il modulo di **adesione stand** (`docs/ADESIONE_STAND.md`) ha una nuova **Sezione E — Energia elettrica**: l'organizzazione fornisce **un solo punto luce** per l'illuminazione dell'area interna dello stand; eventuali **esigenze elettriche aggiuntive** (attrezzatura, potenza assorbita in **kW/kWh**, tipo allaccio) devono essere **elencate nel modulo** e comportano un **contributo economico concordato a carico dello stand** (non incluso nel prezzo di partecipazione); campo firma+data di accettazione. Sezioni successive rinumerate (F Prezzo, G Caparra, H Regolamento, I Esito). Regolamento di manifestazione (`docs/regolamenti/notti-cilentane-2027.md`) aggiornato di conseguenza (Art. 3: punto luce unico fornito dall'organizzatore, autonomia elettrica per il resto). Solo file in `docs/`: nessun deploy su Render.
- Eventi GTM per click di navigazione (frontend, solo dataLayer): `sfe_event_click` (card evento nella home, sezione `home`/`home_past`), `sfe_stand_click` (card stand nel dettaglio evento `event_detail`, marker in mappa `event_map`, chip nel menu `stand_menu_chip`, link nel menu evento `event_menu_stand`) e `sfe_product_click` (apertura dettaglio prodotto nel menu, `stand_menu`/`event_menu_stand`/`event_menu_category`, e aggiunta al carrello `add_to_cart`). Parametri: id/nome leggibile di evento/stand/prodotto, numero e tipo stand, prezzo e id collegamento prodotto–evento, sezione di origine. Maggiorata `docs/GTM_EVENTI.md`. Verifica: `tsc -b` OK, lint nessun errore nuovo (solo i pre-esistenti). Solo frontend: nessuna rigenerazione di `distro/local-app.tar` necessaria.
- **Modulo di adesione stand generato dall'evento** (primo passo dell'adesione elettronica): nuovo modello `AdhesionForm` (una per evento, `eventId` unique) con 11 sezioni HTML — tra cui dati evento, dati stand, prodotti, HACCP, moneta dell'evento, energia elettrica (contributo a carico dello stand), commissioni, prezzo di partecipazione, caparra, regolamento, esito — generate dal servizio `adhesion-form.service.ts` a partire dai dati dell'evento (fingerprint sha256 su configurazione, moneta, fasce; le sezioni `event-header`/`currency`/`fees` sono guidate e sovrascritte a ogni rigenerazione, le altre manuali preservate). API `/api/events/:eventId/adhesion-form`: GET pubblico, POST `/generate`, PATCH, DELETE. **Staleness**: modificare l'evento (PATCH che cambia fingerprint) marca il modulo `stale: true` e la risposta include `adhesionFormStale: true` → `EventsPage` mostra un avviso e la pagina di gestione un banner. Sanitizer HTMl in backend (niente `<table>`), rappresentazione moneta con logo o iniziale in circoletto (mai simbolo nudo). Frontend: pagina stampabile pubblica `AdhesionFormPublicPage` (`/events/:eventId/adhesion-form`, A4 con `@media print`), pagina gestione `AdhesionFormManagePage` (`/admin/events/:eventId/adhesion-form`, genera/rigenera/salva/elimina con RichEditor e badge Auto/Manuale), link "Scarica modulo di adesione" in `EventDetailPage` e "Adesione" sulle card di `EventsPage`. Test backend `integration-adhesion-form.test.ts` (10 test); suite completa backend 313 test OK, frontend build (tsc+vite) e 41 test vitest OK. Il wizard di compilazione elettronica resta in TODO. Nessun tocco a `.local/`: nessuna rigenerazione di `distro/local-app.tar` necessaria.
- **Modulo di adesione — logo ed evidenziazione moneta**: nella generata "Sezione D — Moneta dell'evento" il nome della moneta custom è ora **evidenziato** (`<mark><strong>NOME</strong></mark>`, sfondo ambra) e affiancato dal **logo della moneta** (`currencySymbol.url`, `<img>` width 72) quando presente, altrimenti dall'**iniziale in circoletto** (`Ⓣ`). Il sanitizer backend ora consente i tag `<img>` (src/alt/width/height) e `<mark>` (scheme http/https) così sopravvivono al salvataggio PATCH dalla pagina di gestione; `RichEditor` supporta le immagini (estensione `@tiptap/extension-image`, pulsante "Immagine (URL)") e l'evidenziazione (estensione `@tiptap/extension-highlight`, pulsante "Evidenzia") — utile a tutte le descrizioni HTML. Test: nuovo test nel file adhesion-form (logo + highlight embedded e preservati via PATCH); suite backend 314 test OK, frontend build + 41 test vitest OK. Solo cloud: nessuna rigenerazione di `distro/local-app.tar` necessaria.
- **Promozioni e Coupon**: sistema completo di sconti e promozioni per evento — tre tipi di coupon con codice (invariante case) applicabile in cassa via input manuale o **QR scan**: `discount` (sconto **%** oppure **importo fisso** in crediti, applicato al totale ordine), `product` (prodotto del menu in omaggio — "regalo semplice" oppure **formula combinata** 2x1/3x2 via `formula { paid, total }`, con cap `formulaMaxFree` pezzi gratis per presentazione e resto non multiplo pagato per intero; ogni ordine con coupon = 1 presentazione, `maxPresentations` per promozione, non restituita se l'ordine viene annullato) e `value` (buono valore al riscatto: accredita crediti al cliente via `EventUserTransaction` di tipo `promotion`). Regole: promo opzionalmente legata a uno stand; `expiresAt`; limite per utente `perUserLimit`; **gli sconti percentuali NON sono applicabili a pagamenti in crediti** (blocco server in `createOrder`/`payOrder` e guardia client). Modello `Promotion` + `PromotionUsage` (storico per utilizzo). API `/api/events/:eventId/promotions`: CRUD (guard event-admin / platform-admin), `GET /:promotionId/qrcode`, `GET /:promotionId/usage`, `POST /validate` (auth) e `POST /redeem-value` (riscatto buoni). Integrazione in `createOrder`/`payOrder` (campi `promotionId`/`promotionCode`/`promotionType`/`discountType`/`discountAmount`/`freeUnits` sull'ordine, `PromotionUsage` con `eventUserId` per il per-user limit), resoconti con sezione coupon (`coupons = { totalAppliedOrders, totalDiscountAmount, byPromotion[] }` e `discountAmount` nei totali/summary) e `reset event orders` che azzera `usedCount`. Frontend: componente riusabile `CouponPanel` in cassa (`CashierOrderPage`, `EventCashierPage`) con anteprima sconto `computeCouponDiscount`, riga "Sconto"/"Prodotti in omaggio" su ricevuta (stampa inclusa) e modale conferma; nuova pagina admin `/admin/events/:eventId/promotions` (gestione coupon, QR inline + download, storico utilizzi, attiva/disattiva) e sezione coupon negli importi dei report (`EventReportPage`, `StandOrdersPage`, `EventOrdersPage`). **Gotchas**: sub-router su `/api/events/:eventId/...` richiede `Router({ mergeParams: true })` in Express 5; i filtri campo-vs-campo su path numerici (es. `maxPresentations > usedCount`) vanno scritti con `$expr`. Test: `integration-promotions.test.ts` (27 test); suite backend completa 341 test OK, frontend build (tsc+vite) + 41 test vitest OK, lint senza errori nuovi. Solo cloud: nessuna rigenerazione di `distro/local-app.tar` necessaria.

- **App locale offline — password di sincronizzazione per stand**: ogni `Stand` ha ora una password di sincronizzazione per-stand (campo `Stand.syncPasswordHash`, hash argon2, min 8 max 128 char, default null), nota solo all'admin di piattaforma o di evento, gestita da `PATCH /api/stands/:standId/sync-password` (guard `hasRole(['platform-admin','event-admin'])`, stand-admin → 403; body `{ syncPassword }` stringa → imposta, vuota/null → azzera; risposta `{ item: { id, syncPasswordSet } }`). La lista remota `/sync/events/:eventId/stands` espone `syncEnabled` (senza richiedere la password) così il pannello Sync locale segnala quali stand sono pronti; lo snapshot `GET /sync/events/:eventId/stands/:standId` e il push `POST /sync/push` richiedono l'header **`X-Sync-Password`** (403 se lo stand non ha password configurata, 401 se errata) — il `SYNC_API_TOKEN` globale resta come gate infrastrutturale. L'app locale salva la password in `LocalState.syncPassword` al primo import riuscito (mai esposta in UI, solo `hasSyncPassword` nel meta; endpoint locale `POST /api/sync/password` per salvarla), il push invia `body.standId` + header e il pannello Sync mostra lo stato con i pulsanti Salva password / Sincronizza (import bloccato quando lo stand selezionato non ha `syncEnabled`). UI nel cloud: sezione "Sincronizzazione app locale (notebook)" in `StandManagePage` (solo platform/event-admin per l'evento selezionato). Test: `sync.test.ts` (12) e `stands.test.ts` (23, +5 endpoint password), suite backend **354 test OK**, typecheck backend OK, frontend cloud build + lint OK, locale typecheck + build OK. Commit `baf0f7a` (cloud) e `0d661fd` (locale); `distro/local-app.tar` rigenerata.

- **Wizard di adesione stand elettronico** (completa l'adesione elettronica): nuovo modello `StandAdhesion` (collezione `standadhesions`, unique index `{ eventId, standId }`) con workflow `draft → submitted → approved/rejected` e dati: stand (nome, tipo, banner/logo/copertina, presentazione, contatti), prodotti con allergeni, conferma HACCP, esigenze elettriche (righe con descrizione + kW + tipo allaccio) e accettazione del punto luce unico, accettazioni di **prezzo di partecipazione**, **caparra**, **regolamento**, **clausola di esclusione** e condizioni energia, e **firma** obbligatoria al submit. `Event` ha i nuovi campi informativi `participationFee`/`deposit` (mostrati nel wizard come checkbox di accettazione; il **payment gateway resta fuori scope**). API `/api/events/:eventId/adhesions`: `GET` (lista, admin), `POST`, `GET /mine`, `GET /:adhesionId`, `PATCH`, `POST /:adhesionId/submit`, `POST /:adhesionId/withdraw` (tutte auth + check custom admin/possesso stand, 404 se non autorizzato) e `POST /:adhesionId/approve` / `POST /:adhesionId/reject` (solo event-admin/platform-admin via `hasRole` con body `{ note }` per il rifiuto). Submit validato lato server (400 con lista campi mancanti); edit di una `submitted` la riporta a `draft`; `approved` è bloccata a edit/withdraw; l'approvazione **NON auto-collega** lo stand all'evento (collegamento manuale esistente). Frontend: `StandAdhesionWizardPage` (`/admin/events/:eventId/stand-adhesion`, 6 sezioni, salva bozza/invia/ritira, badge stato) e `AdhesionsManagePage` (`/admin/events/:eventId/adhesions`, ordinata submitted→draft→rejected→approved, dettagli espandibili, Approva e Rifiuta con nota via `ConfirmModal variant="prompt"`); voci dedicate in `AdminSidebar`. Test `integration-stand-adhesions.test.ts` (10 test); suite backend 44 file / **364 test OK**, typecheck + lint backend OK (baseline 33), frontend build (tsc+vite) + 41 test vitest OK, lint frontend senza errori nuovi (baseline 58). Nessun tocco a `.local/`: nessuna rigenerazione di `distro/local-app.tar` necessaria.

- **Fix wizard di adesione + keep-alive frontend su Render**: ① il wizard restava bianco se il fetch di `adhesions/mine` non risolveva — ora i 4 fetch di bootstrap (evento, miei stand, ruoli, adesione) girano in `Promise.allSettled` e `loading` si azzera all'esito di tutti (`StandAdhesionWizardPage.tsx`). ② il dropdown "Adesione Stand" in `EventDetailPage` era interamente nascosto dietro `event.regulationDocument` — ora è sempre visibile e solo la voce "Scarica Regolamento" resta condizionata al PDF. ③ nuovo hook `frontend/src/hooks/useKeepAlive.ts`: ogni 9 min fa `fetch('/', { cache: 'no-store' })` e mantiene sveglio il servizio frontend su Render free (che, a differenza del backend tenuto vivo dallo slideshow, dopo ~15 min di inattività andava in cold start ~50s); usato nella cassa stand (`CashierOrderPage`) e in quella evento (`EventCashierPage`), cioè dove c'è attività operativa. ④ fix test backend `integration-stand-adhesions.test.ts`: `stand` mancante nella destructure di `setupEnvironment()` (ReferenceError) + cast per `delete payload.standId` (TS2790). Test frontend wizard normalizzati da `.tmp` in `StandAdhesionWizardPage.test.tsx`. Verifica: backend typecheck ✓, suite backend 364 test ✓, frontend build (tsc+vite) ✓, 42 test vitest ✓, lint senza errori NUOVI (restano solo i pre-esistenti). Nessun tocco a `.local/`: nessuna rigenerazione di `distro/local-app.tar` necessaria. COMMIT `2af8f9e`.

- **Modulo di adesione pubblico senza login + data ultima saldo quota/caparra**: il wizard di adesione diventa **pubblico e anonimo** (`/events/:eventId/stand-adhesion`, già coperto da PublicLayout) e per gli operatori resta accessibile anche da `/admin/events/:eventId/stand-adhesion`. ① **Gate regolamento**: `createAdhesion` → 400 se l'evento non ha `regulationDocument` (il wizard mostra un avviso e non compila); ② **accesso anonimo**: anche senza sessione si può creare un'adesione per uno **stand nuovo** (senza `standId`, che richiede invece admin/owner) — ogni adesione genera un **access token** (random 32 byte, hash sha256 salvato in `accessTokenHash`, campo nuovo su `StandAdhesion`) restituito nella response al create e conservato dal browser in `localStorage` (chiave `sfe_adhesion_access_token_<eventId>`) e inviato via header `x-access-token` (helper `getsTokenHeaders` nel wizard); `GET /adhesions/mine` e le operazioni get/patch/submit/withdraw sono ora su `optionalAuthMiddleware` e risolvono l'autorizzazione anche col token (fallback all'utente autenticato con ruoli stand/evento). ③ **creazione utente al submit** per i nuovi stand: da `contactName`/`contactEmail` viene creato (o riusato se l'email esiste già) un utente **inattivo** con token di attivazione e invito email (`ensureOwnerUser` riusa `generateActivationToken` + `sendActivationEmail`); la risposta del submit restituisce `activationUrl` (se l'email non è inviabile) e `emailSent`; `StandAdhesion.userId` (campo nuovo) collega l'adesione all'utente. ④ **creazione dello stand SOLO all'approvazione**: `approveAdhesion` (ora limitata a **event-admin**, escluso platform-admin — `hasRole(['event-admin'])` matcha per slug) quando l'adesione non ha `standId` crea lo `Stand` con `type`/`name`/`slogan`/`description`, `coverImage` = banner dell'adesione, `logo`, `eventIds`, **numero progressivo per evento** (`nextStandNumber` estratto in `utils/stand-number.ts`) e assegna il ruolo **stand-admin** (scope stand) all'utente dell'adesione (se presente); nessun `EventProduct` importato. La completezza del submit per i nuovi stand richiede anche `contactName` + `contactEmail`. ⑤ **scadenze pagamenti**: nuovi campi informativi su `Event` — `participationFeeDeadline` e `depositDeadline` (Date, default null) accanto a `participationFee`/`deposit`, gestiti in create/update/response di `events.controller` (il **duplicato evento NON li copia**: nuova edizione → null) e nel form di `EventsPage` (input `type="date"`, payload ISO); il wizard li mostra accanto a quota e caparra (`saldo entro il …`). Verifica: backend typecheck ✓, suite backend **372 test** ✓ (nuovi: anonimo create+accessToken, anonimo con standId → 403, evento senza regolamento → 400, mine via token, submit nuovo stand → utente inattivo creato/riusato, approve → stand creato+numero+ruolo stand-admin, platform-admin → 403, round-trip scadenze), frontend build (tsc+vite) ✓, **43 test vitest** ✓ (mock wizard con regolamento + notice senza regolamento), lint senza errori NUOVI (baseline invariata). Nessun tocco a `.local/`: nessuna rigenerazione di `distro/local-app.tar` necessaria.

- Parametri coupon negli eventi GA4/GTM: `sfe_cashier_order_created` e `sfe_order_created` ora trasportano `promotion_code`, `discount_amount` e `coupon_type`; `sfe_coupon_applied` ora include i reali `discount_amount` e `freeUnits` calcolati da `computeCouponDiscount` (prima restavano sempre a 0). Solo frontend: `analytics.ts` (+3 parametri per event order + fix coupon), `CouponPanel.tsx` (compute discount al validation time), `CashierOrderPage.tsx` e `EventCashierPage.tsx` (passano valori dalla response API).
- **Fix "Stand collegato" per nome nel wizard adesione**: il campo "Stand collegato" dei gestori evento accettava solo l'ObjectId, ma l'operatore digita il **nome** dello stand → `Cast to ObjectId failed` alla creazione/modifica dell'adesione. Il backend ora risolve il riferimento (`resolveStandReference`) cercando lo stand per **nome** (case-insensitive) tra gli stand dell'evento, oltre che per id; nome inesistente → 400 "Nessuno stand trovato…". Il frontend converte il campo da free-text in `<select>` (solo stand GIÀ registrati di proprietà dell'utente o dell'evento per i gestori; prima opzione "— Nuovo stand: scrivi il nome qui sotto —"), precompila `standName` alla selezione e svuota "Nome attività" alla deselezione — il modello rimane: nuova adesione = nuovo stand o stand esistente di proprietà, il nome del NUOVO stand si scrive solo nella prima riga. Test: +2 in `integration-stand-adhesions.test.ts`, suite backend **374 test OK**, typecheck + lint OK, frontend build + lint OK. Nessun tocco a `.local/`: nessuna rigenerazione di `distro/local-app.tar` necessaria.
- **Fix submit adesione**: ① il backend validava `participationFeeAccepted`/`depositAccepted` **incondizionatamente**, ma se l'evento non ha `participationFee`/`deposit` (gezione) quelle accettazioni non vanno richieste — ora `completenessErrors` riceve l'evento e richiede le accettazioni solo se quota/caparra sono definite (stessa regola già applicata lato UI e `missing` list). ② il wizard inviava il submit coi dati salvati in precedenza, ignorando modifiche recenti (es. firma compilata dopo l'ultimo salvataggio) → `handleSubmit` ora fa sempre `persist(buildPayload(form))` (create o PATCH a seconda dello stato) prima del POST `/submit`. Test: +1 in `integration-stand-adhesions.test.ts` (evento senza quota/caparra → submit OK senza acceptance), suite backend **375 test OK**, typecheck + lint OK, frontend build + lint OK. Nessun tocco a `.local/`: nessuna rigenerazione di `distro/local-app.tar` necessaria.
- **Stato "Da integrare" (adesioni stand)**: l'event-admin può richiedere integrazioni su un'adesione inviata con il nuovo terzo pulsante "Da integrare" (richiede una nota, `POST /:adhesionId/integration`, solo event-admin). La richiesta porta l'adesione nello stato `integration` e resetta `submittedAt`+`reviewNote`; lo stand la riapre in modifica dal wizard (stato `editable` come draft/rejected), la corregge e la reinvia (`submit` azzera `reviewedAt`/`reviewNote`); può anche ritirarla. Il gestore può da una richiesta "da integrare" approvare/rifiutare/chiedere ulteriori integrazioni. Badge blu "Da integrare" + contatore in header. Test: +1 in `integration-stand-adhesions.test.ts` (submit → integration con nota → modifica/risubmit), suite backend **377 test OK**, typecheck + lint OK, frontend build (tsc+vite) ✓, lint 0 errori (13 warning pre-esistenti). Nessun tocco a `.local/`: nessuna rigenerazione di `distro/local-app.tar` necessaria.
- **Referente dello stand nel wizard adesione**: i campi referente (nome e cognome in campo unico, email, telefono) vengono estratti dalla sezione ① Dati dello stand e spostati in una sezione dedicata finale ⑦ "Referente dello stand", con il nuovo campo **recapito social** opzionale (`contactSocial`). Validazione allineata frontend/backend: nome referente **obbligatorio per tutti**; per gli stand **nuovi** (standId vuoto) l'**email è obbligatoria** (serve per creare l'account di gestione che riceve l'invito di attivazione); per gli stand **già registrati** basta **email O telefono**. Il modello `StandAdhesion` e la risposta API espongono `contactSocial`. Test: +1 in `integration-stand-adhesions.test.ts` (nome mancante → 400; stand collegato senza email/telefono → 400; solo telefono → OK; stand nuovo senza email → 400), suite backend **377 test OK**, typecheck + lint OK, frontend build (tsc+vite) ✓, lint 0 errori (13 warning pre-esistenti). Nessun tocco a `.local/`: nessuna rigenerazione di `distro/local-app.tar` necessaria.
