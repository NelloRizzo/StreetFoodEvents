services:
  # --- Backend API ---
  - type: web
    name: streetfoodevents-api
    env: node
    region: frankfurt
    plan: free
    rootDir: backend
    buildCommand: npm ci && npm run build
    startCommand: npm run start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: '4000'
      - key: MONGODB_DB_NAME
        value: street-food-events
      - key: CLIENT_URL
        value: https://streetfoodevents.onrender.com
      - key: MONGODB_URI
        sync: false
      - key: CLOUDINARY_CLOUD_NAME
        sync: false
      - key: CLOUDINARY_API_KEY
        sync: false
      - key: CLOUDINARY_API_SECRET
        sync: false
      - key: BREVO_API_KEY
        sync: false
      - key: EMAIL_FROM
        sync: false
      - key: EMAIL_MESSAGE_TEMPLATE
        sync: false

  # --- Frontend (SPA) ---
  - type: web
    name: streetfoodevents
    env: node
    region: frankfurt
    plan: free
    rootDir: frontend
    buildCommand: npm ci --include=dev && npm run build
    startCommand: npx serve -s dist -l $PORT
    envVars:
      - key: NODE_ENV
        value: production
      - key: VITE_BASE_URL
        value: /
      - key: VITE_API_URL
        value: https://streetfoodevents-api.onrender.com/api
      - key: VITE_GTM_ID
        sync: false
      - key: VITE_PRIVACY_EMAIL
        sync: false
